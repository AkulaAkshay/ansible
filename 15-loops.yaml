- name: loops
  hosts: local
  connection: local
  tasks:
  - name: print names
    ansible.builtin.debug:
      msg: "Hello {{ item }}" #here "item" keyword (special keyword ansible provides us by default) is reserved
    loop:
    - ramesh
    - suresh
    - ramu
    - raheem