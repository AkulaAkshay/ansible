- name: loops
  hosts: local
  connection: local
  tasks:
  - name: print names
    ansible.builtin.debug:
      msg: "Hello {{ item }}" #here "item" keyword is reserved
    loop:
    - ramesh
    - suresh
    - ramu
    - raheem