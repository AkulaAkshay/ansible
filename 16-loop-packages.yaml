- name: loops
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: install multiple packages by using loops
    ansible.builtin.package:
      name: "{{ item }}"
      state: latest
    loop:
    - zip
    - nginx
    - nodejs
    - mysql