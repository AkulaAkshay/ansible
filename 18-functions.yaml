- name: functions
  hosts: local
  connection: local
  tasks:
  - name: default variable
    ansible.builtin.debug:
      msg: "Hello {{ name | default('RAMESH') }}"


- name: functions
  hosts: local
  connection: local
  vars:
    courses: ansible, linux, terraform, k8
  tasks:
  - name: default variable
    ansible.builtin.debug:
      msg: " courses learnt are: {{ courses | split (',') }}"
      